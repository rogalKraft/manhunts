# Put @s back on the locator bar. Run over EVERY player at match end, not just role
# holders, so someone whose role changed mid-match cannot stay invisible afterwards.
attribute @s minecraft:waypoint_transmit_range modifier remove manhunt:match
